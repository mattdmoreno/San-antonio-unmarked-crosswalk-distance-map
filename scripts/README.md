Scripts

- `get-seattle-osm-pbf.sh`: downloads Seattle extract from BBBike
- `build-seattle-basemap.sh`: builds `basemap-seattle.pmtiles` using Planetiler (Docker)
- `serve-data.sh`: serves `./data` locally for PMTiles testing
